plugins {
    id 'application'
}

dependencies {
    implementation project(':lib-core')
    // The node-API transport the wallet drives, plus lib-net's deadline/bounded-read machinery
    // (BodyReadDeadline, PeerExchange) it is built on.
    implementation project(':lib-net')

    implementation "org.json:json:${jsonVersion}"
    // NodeHttpClient logs the cleartext-token guards directly.
    implementation "org.slf4j:slf4j-api:${slf4jVersion}"

    runtimeOnly "ch.qos.logback:logback-classic:${logbackVersion}"

    // The end-to-end wallet test drives a real node.
    testImplementation project(':app-node')
    testImplementation project(':lib-persistence')
}

application {
    mainClass = 'rhizome.wallet.WalletCli'
}

tasks.named('test') {
    // Use JUnit Platform for unit tests.
    useJUnitPlatform()
}
